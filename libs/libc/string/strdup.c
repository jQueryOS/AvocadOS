#include <string.h>

size_t strdup(const char* source) {
//TODO: malloc (ouch)
/*	int length = strlen(source);
	//const char *array = malloc(length);
	const char* result = array;
	for (int i = 0; i < length; i++)
		result[i] = source[i];
	return result;*/
//TODO
	return source;
}
